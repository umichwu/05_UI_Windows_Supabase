# Ensure consistent package resolution
node-linker=hoisted
# Force complete package installation
prefer-offline=false
package-lock=true
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
